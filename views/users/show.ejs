<!DOCTYPE html>
<html lang="en">
<head>
    <%- include ('../partials/header.ejs') %>
</head>
<body>
    <div class="container">
        <h1><%= user.name %></h1>
        <h3>Hi <%=user.nickname%>!</h3>
        <h3>Here's your current collection of movies:</h3>
        <ul>
            <% if (user.movieCollection.length >0 ) {for (let i=0; i<user.movieCollection; i++){ %>
                <li>
                    <%= user.movieCollection[i].fullTitle %>
                </li>
            <%} } else { %>
                You don't have any movies in your collection yet, <a href="#">add some here!</a>
            <% } %>
        </ul>
        <a href="/users/<%=user.id%>/edit">Edit User Info</a>
        <a href="/users" >Return to User List</a>
        <a href="/" >Return to Home Page</a>
        <form action="/users/<%=user.id%>?_method=DELETE" method="POST">
        <input type="submit" value="Delete User" />
        </form>
    </div>
</body>
</html>